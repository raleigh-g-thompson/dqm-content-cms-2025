+++
id = "C-05"
title = "CMS157 Pain Intensity Quantified — RESOLVED on CMS engine: prior 'fixture MR/Encounter-type authoring mismatch' conclusion RETRACTED (root cause was C-02 truncated 'Cancer' valueset); residual QICore column is stale-baseline artifact pending QICore re-run"
category = "content"
status = "Fixed — verified 63/0 on CMS engine (2026-09-07) and QICore engine (2026-09-08)"
resolved = true
root_cause_status = "retracted — prior authoring-mismatch diagnosis disproven; real cause = C-02 valueset truncation"
workaround = "None needed — valuesets repaired via scripts/comparison/repair_valueset_cache.py; re-run the QI-Core engine (cql.explorer.project.execute-all in the QICore repo) and regenerate qicore-2025-actual-results.csv to close the residual classification"
affected_measures = ["CMS157FHIRPainIntensityQuantified"]

[references]
defect_tracking = ["defect-tracking catalog (CMS157 fixture MR authoring mismatch)"]
issues = ["C-02", "C-03", "C-04"]
+++
### C-05: CMS157 Pain Intensity Quantified — RESOLVED on CMS engine; prior authoring-mismatch diagnosis RETRACTED

- **REVISED 2026-09-07 (post-valueset-repair engine run) — prior diagnosis retracted**: the earlier
  claim that the fixture Encounters carry `type` (or no `type`) that doesn't match the
  `[Encounter: "Office Visit"]` / `[Encounter: "Audio Visual Telehealth Encounter"]` valuesets was
  **wrong**. The 19 fixtures were correct all along; the actual (and only) blockage was the truncated
  "Cancer" valueset (C-02): its empty expansion made `[Condition: "Cancer"]` match nothing, so
  Initial Population evaluated 0 regardless of the Encounters. After the C-02 repair, the CMS engine
  returns exactly the expected counts on **all 63/63** CMS157 test cases (only the Cancer valueset
  `2.16.840.1.113883.3.526.3.1010` was repaired; Office Visit / Audio Visual Telehealth / Chemotherapy
  Administration valuesets were never truncated).
- **Residual (RESOLVED 2026-09-08)**: the previously-flagged QI-Core column (CMS actual=expected vs
  QI-Core=0) and the 46 `qicore-only` cells were **stale-baseline artifacts** — `qicore-2025-actual-results.csv`
  (`2026-09-05`) and the QI-Core `TestCaseResult-*.json` set predated the valueset repair, and the
  QI-Core repo carried the identical 45 truncated files. The QI-Core engine was re-run 2026-09-08 and
  the regenerated baseline flips the CMS157 QICore column to 63 / 0 (Match) — no residual cells.
- **Previous symptom record (historical)**: 19 failing test cases / 46 mismatched cells (Group_1:
  Initial Population, Denominator, plus 8 Numerator). Both engines returned IP=0; expected=1;
  Denominator mirrored; Numerator mirrored on 8/19 cases.
- **Historical mechanism (now disproven)**: `[Encounter: "Office Visit"] union [Encounter: "Audio Visual
  Telehealth Encounter"]` plus `[Procedure: "Chemotherapy Administration"]` and `[Condition: "Cancer"]`;
  the inference that fixture Encounters carried no matching `type` code was incorrect.
- **Category**: terminology / stale-content (folded into C-02), not fixture authoring.
