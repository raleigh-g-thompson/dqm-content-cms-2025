+++
id = "E-20"
title = "`(never assigned)` — intentional ID gap in the engine issue series"
category = "engine"
status = "**never assigned**"
defect_status = "retired"
root_cause_status = "retired"
workaround = "—"
affected_measures = []
+++
### E-20: tombstone (never assigned)

E-20 was never used. `git log --all -S'E-20'` returns zero commits across every
branch and file — the number was skipped, not lost. No code, case mapping, or
historical record ever referenced an E-20 issue, so this slot is preserved as a
permanent gap rather than filled with a renumbered issue. The lineage continues
normally at E-21.